# Git Practice

My first repo for learning Git and GitHub basics.

## What I learned
- Git workflow (add, commit, push)
- Branching and merging
- Merge conflicts
- SSH keys
- Pull requests
```

Save it, then:
```
git add .
git commit -m "add README, closes #1"
git push